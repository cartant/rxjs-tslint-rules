import { of } from "rxjs";

class SomeClass {
  value: string;
  something() {
    of("foo").subscribe(value => this.value = value);
  }
}

function someFunction() {
  of("foo").subscribe(value => this.value = value);
}
